# WhatsApp Bridge Environment Variables

# Server Configuration
PORT=8080
GIN_MODE=release

# Optional: Custom database path (defaults to store/whatsapp.db)
# DB_PATH=./store/whatsapp.db

# Optional: Custom message database path (defaults to store/messages.db)  
# MESSAGE_DB_PATH=./store/messages.db

# Optional: Log level (DEBUG, INFO, WARN, ERROR)
# LOG_LEVEL=INFO